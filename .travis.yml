language: cpp
compiler: gcc
os: osx
before_install:
  - pip install --user cpp-coveralls
script:
  - cmake -DCMAKE_CXX_FLAGS="-ftest-coverage -fprofile-arcs" .
  - make
  - ./GTests
after_success:
  ~/Library/Python/2.7/bin/cpp-coveralls --root . -E ".*CMakeFiles.*" -i srcs -i includes
